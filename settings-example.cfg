[DEFAULT]

# The resolution of the trailer file to download.  Valid values are 480, 720, and 1080.
# Higher values are better quality, but much larger files
# Defaults to 720
resolution = 720

# The directory that the files should be downloaded into.
# Defaults to the directory the script is in.
download_dir = /tmp/
